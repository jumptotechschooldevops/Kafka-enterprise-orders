apiVersion: 1

providers:
  - name: "Kafka Enterprise Orders"
    orgId: 1
    folder: "Kafka"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
